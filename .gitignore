# Build artefacts
/xhelix
/xhelixctl
/xhelix-verify
/xhelix-bridge
/mynetgate
/xhelix-dnspoison
/xhelix-honeysh
/xhelix-sinkhole
/xhelix-watchdog
/xhelix-graph-server
/dist/
*.deb
*.rpm
*.tar.gz
*.o
# Deb staging binaries are populated by `make deb`; not tracked.
packaging/deb/usr/local/bin/xhelix
packaging/deb/usr/local/bin/xhelixctl
packaging/deb/usr/local/bin/xhelix-verify

# Go
/vendor/

# IDE / tools
.vscode/
.idea/
.claude/

# Documentation — all .md files excluded from git tracking
# (operator preference: docs are private; design/strategy/phase
# reports live on disk only). No whitelist exceptions.
*.md

# Local state for development
/var/
/tmp/
*.db
*.db-journal
*.db-shm
*.db-wal

# OS
.DS_Store
Thumbs.db

# Backups
.git-old-history-backup/
